\chapter{Extraits de configuration}
\label{annexe:config}

% TODO : coller ici les extraits de configuration les plus
% représentatifs (docker-compose.yml, Jenkinsfile, règles UFW...),
% en gardant les valeurs sensibles hors du rapport (tokens, mots
% de passe).

\section{docker-compose.yml (extrait)}
\begin{lstlisting}[language=bash]
% TODO : coller un extrait pertinent (ex. labels Traefik)
\end{lstlisting}

\section{Jenkinsfile (extrait)}
\begin{lstlisting}[language=bash]
% TODO : coller un extrait pertinent (ex. étape Test)
\end{lstlisting}

\section{Règles UFW}
\begin{lstlisting}[language=bash]
% TODO : coller les règles UFW appliquées
\end{lstlisting}
